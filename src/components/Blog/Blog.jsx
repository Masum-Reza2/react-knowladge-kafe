import PropTypes from 'prop-types'; // ES6
const Blog = ({ blog }) => {
    return (
        <div>

        </div>
    )
}

Blog.propTypes ={
    blog : PropTypes.object.isRequired,
}
export default Blog